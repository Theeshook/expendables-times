# The Expendables Times

The weekly paper for a private fantasy Premier League. The site is a single
self-contained HTML page, rebuilt every Tuesday from live data and published here.

Built from the `Expendables FPL` project: prose lives in `brief.template.html`,
figures are generated by `build.mjs`, and nothing is published until `verify.mjs`
traces every claim back to a source.
